[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "tableflow"
version = "1.0.0"
description = "Reusable middleware engine for search, filter, pagination"
authors = [{name="swe-shree"}]
