[project]
name = "tableflow"
version = "0.1.0"
dependencies = ["typer"]

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project.scripts]
tableflow = "tableflow.cli:app"