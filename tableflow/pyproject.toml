[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "tableflow"
version = "1.0.0"
description = "SaaS-ready middleware engine for search, filtering, pagination"
authors = [{name="swe-shree"}]

dependencies = []
