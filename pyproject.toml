[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "tableflow"
version = "0.1.0"
description = "Smart middleware for search, filtering, pagination"
authors = [{name="swe-shree"}]

dependencies = []